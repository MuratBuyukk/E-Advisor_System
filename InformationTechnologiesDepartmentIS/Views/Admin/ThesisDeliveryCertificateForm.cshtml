@model List<InformationTechnologiesDepartmentIS.Models.FormThesisDeliveryCertificate>

@{
    ViewBag.Title = "ThesisDeliveryCertificateForm";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container-fluid px-4 mt-4">
    <div class="card mb-4 mt-4">
        <div class="card-header alert alert-info h5">
            <i class="fas fa-table me-1"></i>
            Thesis Delivery Certificate Forms
        </div>
        <div class="card-body">
            <table id="datatablesSimple">
                <thead>
                    <tr>
                        <th>Number</th>
                        <th>Student</th>
                        <th>Form Date</th>
                        <th>Status</th>
                        <th>View</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var project in Model)
                    {
                        <tr>
                            <td>@project.MasterThes.Student.StudentNumber</td>
                            <td>@($"{@project.MasterThes.Student.StudentFirstName} {@project.MasterThes.Student.StudentLastName}")</td>
                            <td>@project.FormDate.Value.ToString("dd.MM.yyyy")</td>
                            @if (project.FormStatus.StatusName == "Pending")
                            {
                                <td><a class="text-warning fw-bold text-decoration-none">Pending</a></td>
                            }

                            else if (project.FormStatus.StatusName == "Accepted")
                            {
                                <td><a class="text-success fw-bold text-decoration-none">Accepted</a></td>
                            }

                            else if (project.FormStatus.StatusName == "Rejected")
                            {
                                <td><a class="text-danger fw-bold text-decoration-none">Rejected</a></td>
                            }
                            else
                            {
                                <td><a class="text-info fw-bold text-decoration-none">Approved</a></td>
                            }
                            <td><a href='/Admin/ThesisDeliveryCertificateFormDetail/@project.FormId'><input type="button" value="View" class="btn btn-primary btn-sm" /></a></td>

                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>


